package com.ocr.ardoise;

import java.awt.Graphics;

public class Rond implements Shape {

	// method to paint the ROND shape :
	public void paintShape(Graphics g, Point pt) {
		
	    // On redéfinit une couleur pour notre rond
	  //  g.setColor(pt.getColor());
	    // On le dessine aux coordonnées souhaitées
	    //g.fillOval(pt.getPosX(), pt.getPosY(), pt.getTailleW(), pt.getTailleH());
	}
}
